#include <stdio.h>
#include <conio.h>
#include <dos.h>

int main (void)
{
    int n[10];
    int i = 0;
    float = promedio;
    clrscr();

    for (i = 0; i < 10 ; ++i)
    {
        printf("Ingrese un numero: ");
        scanf("%d",&n[i]);
    }

	delay(700);
	clrscr();
	printf("Los numeros son:\n\n ");

    for (i = 0; i < 10; ++i )
        printf("Numero %d \n", n[i]);

    promedio = 

    printf("%f",promedio);

    getch();
    return 0;
}